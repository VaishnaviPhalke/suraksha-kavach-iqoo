package com.suraksha.kavach.ui.theme

import androidx.compose.ui.graphics.Color

/**
 * Suraksha Kavach Light Theme Color System
 * 
 * Aesthetic Philosophy:
 * Minimal, friendly, cute, human-designed consumer utility.
 * Strictly avoids dark hacker tropes, high-gloss gradients, and aggressive tech clutter.
 */
val WarmOffWhite = Color(0xFFFDFBF7)       // Main app background
val CardWhite = Color(0xFFFFFFFF)          // Card surface
val BorderGray = Color(0xFFE2E8F0)         // Subtle 1dp border for 20dp cards
val SageGreenSafe = Color(0xFF10B981)      // Primary safe accent (healthy shield, verified steps)
val SageGreenLight = Color(0xFFECFDF5)     // Light sage background tint
val LavenderBlue = Color(0xFF6366F1)       // Soft lavender for system tags & NPU labels
val LavenderBlueLight = Color(0xFFEEF2FF)  // Light lavender background tint
val SoftAmber = Color(0xFFF59E0B)          // Warning/caution & risk indicators
val SoftAmberLight = Color(0xFFFEF3C7)     // Light amber container
val SoftRose = Color(0xFFF43F5E)           // Revocation & emergency purge action
val SoftRoseLight = Color(0xFFFFE4E6)      // Light rose container
val TextCharcoal = Color(0xFF1E293B)       // Primary readable typography
val TextMuted = Color(0xFF64748B)          // Subtitles & metadata
val TextLight = Color(0xFF94A3B8)          // Disabled or placeholder text
val SurfaceTint = Color(0xFFF8FAFC)        // Subtle pill container
